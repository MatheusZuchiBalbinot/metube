name: Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  trivy:
    name: Trivy Filesystem Scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Scan the repo filesystem for known vulnerabilities in dependencies
      # AND for IaC/Dockerfile/compose misconfigurations. Only HIGH/CRITICAL
      # findings fail the job so low-severity noise does not block merges.
      - name: Run Trivy vulnerability & misconfiguration scan
        # Pinned to v0.36.0 (immutable commit SHA) instead of @master to avoid
        # running a mutable third-party ref in CI.
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          scan-type: fs
          scan-ref: .
          scanners: vuln,misconfig,secret
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: '1'
          format: table
