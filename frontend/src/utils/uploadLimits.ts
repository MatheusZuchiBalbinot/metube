/**
 * Frontend mirror of the size limits in backend/app/Config/UploadLimits.php
 * — kept in sync by hand. The backend re-validates size regardless.
 */

export const VIDEO_MAX_SIZE_MB = 2048;
export const THUMBNAIL_MAX_SIZE_MB = 10;
